# PowerCare Design System (SAP Fiori–style)

المرجع الأول قبل بناء أي شاشة جديدة. جميع القيم أدناه مطبّقة فعليًا في `src/index.css` كمتغيرات CSS.

## §1 الألوان

| الدور | القيمة | متغيّر / كلاس |
|---|---|---|
| Primary (أزرق مؤسسي) | `#0052CC` | `--primary` · `bg-primary` |
| Primary Dark (الشريط الجانبي) | `#003A99` | `--sidebar-background` |
| Accent / Success (أخضر حيوي) | `#00875A` | `--accent` · `bg-accent` |
| Background | `#F5F6FA` | `--background` |
| Card | `#FFFFFF` | `--card` |
| Border | `#D0E3FF` | `--border` |
| Muted text | `#5A6474` | `--muted-foreground` |
| Destructive | `#CC1F1A` | `--destructive` |
| Table header | `#F5F6FA` | thead |
| Row hover | `#EEF3FF` | tbody tr:hover |

قاعدة: لا تُكتب ألوان حرفية في JSX — استخدم كلاسات التوكِن (`bg-primary`, `text-accent`, `border-border`).
الوضع المظلم مدعوم عبر `.dark` بنفس أسماء المتغيرات.

## §2 الخطوط

- `IBM Plex Sans Arabic` للعناوين والواجهة (`font-heading`, `font-body`).
- الجداول: 13px · النصوص: 15px · العناوين الرئيسية: 22–28px.

## §3 تتالي المحتوى الثابت لكل شاشة

1. `PageHeader` (عنوان + وصف + أزرار)
2. شريط التصدير (Export bar)
3. مؤشرات KPI (بطاقات أفقية)
4. الجدول أو القائمة الرئيسية
5. القسم التفصيلي / الدريل داون
6. بطاقة القاعدة أو الملاحظات

انسخ هذه الفقرة و§4 كتعليق في رأس أي ملف شاشة جديد.

## §4 أنماط المكونات

- **الجداول:** `thead` بخلفية `#F5F6FA`، uppercase 11px بوزن متوسط، صفوف بيضاء مع تظليل بديل خفيف، `border-bottom: 1px #E0E7F5`، hover `#EEF3FF`، أزرار الإجراءات صغيرة في نهاية الصف.
- **الشريط الجانبي:** خلفية `#003A99`، العنصر النشط بخلفية `#0052CC/15%` وحد جانبي 3px بلون `#00875A`، أيقونات خضراء.
- **بطاقات KPI:** بوردر أزرق فاتح، القيمة الرئيسية بالأزرق، الملاحظة بالرمادي، خط سفلي أخضر 2px.
- **أشجار الهيكل (HR Tree):** خطوط توصيل رمادية، عقد مستطيلة `radius: 6px`، العقدة النشطة `#EEF3FF`.

## §5 التخطيط و RTL

- الشريط الجانبي على اليمين دائمًا في RTL (`right-0`) وحدّه `border-inline-start`.
- المحتوى الرئيسي يمتد على اليسار، والأيقونات والأزرار الإجرائية في نهاية اليسار.
- استخدم دائمًا الخصائص المنطقية (`ms-`, `me-`, `start`, `end`) بدل `left/right`.

## §6 الموقع التعريفي (Landing)

- Hero بخلفية بيضاء وعنوان رئيسي بالأزرق الداكن `#003A99`.
- أزرار CTA بالأخضر `#00875A`.
- بطاقات الخدمات ببوردر أزرق فاتح `#D0E4FF`.
- قسم الأسعار بتصميم ثلاثي الأعمدة بأسلوب SAP.