{
  "name": "EmployeeCredential",
  "type": "object",
  "properties": {
    "companyId": {
      "type": "string",
      "description": "The PowerCare company (tenant) this credential belongs to."
    },
    "employeeId": {
      "type": "string",
      "description": "Stable client-side employee id (matches Employee.employeeId)."
    },
    "email": {
      "type": "string",
      "description": "Lowercased login email."
    },
    "passwordHash": {
      "type": "string",
      "description": "Salted SHA-256 hash (sha256$<salt>$<hex>) \u2014 never plaintext."
    }
  },
  "required": [
    "companyId",
    "employeeId",
    "email",
    "passwordHash"
  ],
  "rls": {
    "create": {
      "user_condition": {
        "role": "admin"
      }
    },
    "read": false,
    "update": {
      "user_condition": {
        "role": "admin"
      }
    },
    "delete": false
  }
}