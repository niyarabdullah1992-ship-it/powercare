# CLAUDE.md — PowerCare / NiroVera

هذا المستودع هو **منصة نيروفيرا التشغيلية** تحت مظلة **PowerCare**.
عند البناء أو التعديل: اربط الفكرة كاملة، ولا تعامل الصفحات كشاشات منفصلة.

## الفكرة المركزية — دورة الإثبات (The Proof Cycle)

المنصة لا تكتفي بتسجيل البيانات. تُثبت أن العمل أُنجز فعلًا عبر سلسلة مترابطة:

1. **حضور موثّق** → الشخص + الموقع + الوقت  
2. **مهمة بوزن جهد** → تنفيذ ميداني + إثبات + إقرار  
3. **اعتماد متسلسل** → مدير ← HR مع سبب مكتوب عند الرفض  
4. **ختم للعميل** → إثبات عمل قابل للتحقق (بصمة / Secure Sign)

أي ميزة جديدة يجب أن تجيب: *أين تقع في هذه الدورة؟ وكيف تتبادل البيانات مع الحلقة السابقة/التالية؟*

## طبقات النظام المترابطة

| الطبقة | ماذا تضم | لا تكسر الربط مع |
| --- | --- | --- |
| الأشخاص | موظفون، هيكل، حضور، طلبات | المهام والمسير |
| العمليات | مهام، تقرير يومي، عهد، مصروفات، سلامة | الحضور والإثبات |
| الثقة | مستندات، توقيع، إثبات عميل، بلاغات، شكاوى | الاعتماد والصلاحيات |
| الإدارة | تنفيذي، مساعد، تقارير، لوحة مالك | مؤشرات الطبقات كلها |

## مبادئ Claude في التصميم (الأفكار لا النسخ البصري)

طبق **أفكار** Claude — لا تنسخ ألوان Claude.ai حرفيًا، ولا تُسطّح هوية المنتج:

1. **هدوء القرار** — أول ما يراه المستخدم: ما يحتاج فعلًا الآن، لا زخرفة.  
2. **تسلسل بصري واضح** — عنوان واحد قوي، جملة دعم قصيرة، ثم المحتوى.  
3. **مساحة كافية** — لا تكدّس بطاقات/إحصائيات في الـ hero أو أول شاشة بلا سبب.  
4. **لمسة لون واحدة مقصودة** — هوية المنتج: كحلي عميق + أخضر سعودي كـ accent محسوب (انظر `src/index.css`).  
5. **طباعة تقود الواجهة** — `IBM Plex Sans Arabic` + `Inter Tight` (من handoff Claude Design)؛ الوزن والحجم قبل الألوان الصارخة.  
6. **ترابط يُروى** — إن أضفت شاشة، أظهر كيف تتصل بدورة الإثبات (نص قصير أو مؤشر حالة).  
7. **RTL عربي أولًا** — الواجهة والتقارير والأخطاء بالعربية أولًا، ثم تعدد اللغات الموجود.

## Handoff Claude Design (مصدر الحقيقة البصري)

- **المرجع الوحيد:** [`design/NiroVera HR System (standalone).html`](design/NiroVera%20HR%20System%20(standalone).html)
- **خريطة الشاشات ↔ الملفات:** [`design/SCREEN-MAP.md`](design/SCREEN-MAP.md)
- طبّق شاشة واحدة في كل مرة حتى التطابق البصري جنبًا إلى جنب مع المنصة على `http://127.0.0.1:5173/`
- هوية الألوان: `#0B1A3F` / `#0E7A4B` / `#F7F8FA` — خطوط `IBM Plex Sans Arabic` + `Inter Tight`
- البيانات تبقى حية عبر Base44؛ لا تنسخ بنية `sc-for` من النموذج
- الملفات القديمة تحت `design-handoff-claude/` أرشيف فقط — لا تعتمدها كمرجع أساسي

## الهوية

- **PowerCare**: المنظومة / الشركة / الجلسة (`powercare_session`, PowerCareAuth).  
- **NiroVera**: اسم المنتج الظاهر للمستخدم في أجزاء من الواجهة والشعار.  
- الشعار: `src/lib/brand.js` → `POWERCARE_LOGO_URL`.  
- لا تستبدل الهوية بثيمات عامة (بنفسجي، توهج، بطاقات عشوائية).

## قواعد تقنية سريعة

- المصدر الأمامي: `src/` — الصفحات في `src/pages/`، المنطق في `src/lib/`.  
- Backend: Base44 (`base44/`, `@base44/sdk`). للتطوير الكامل: `base44 dev`. للواجهة فقط: `npm run dev`.  
- لا تكتب أسرارًا في الكود؛ استخدم `.env.local`.  
- احترم `navVisibility` والصلاحيات والخطط قبل إظهار مسارات جديدة.  
- دورة المهام/التصعيد: `MyTasks.jsx`, `src/lib/escalation.js`.  
- إثبات العميل والتوقيع: `ClientProof.jsx`, `FileSigning.jsx`, `drawHeritageFingerprint.js`.

## عند طلب المستخدم «اربط الفكرة» أو «مثل Claude»

1. افتح `design/NiroVera HR System (standalone).html` واقرأ الشاشة المطلوبة.  
2. راجع `design/SCREEN-MAP.md` لمعرفة ملف `src/` المقابل.  
3. حدّث التدفق بين الصفحات/البيانات لا الشكل فقط.  
4. طبّق مبادئ الهدوء والتسلسل والترابط.  
5. أبقِ الهوية PowerCare/NiroVera كما في المشروع.

## أوامر

```bash
npm install
npm run dev          # واجهة فقط
base44 dev           # واجهة + خلفية Base44 المحلية
```
